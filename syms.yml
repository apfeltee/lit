  # void lit_init_api(LitState *state);
  "lit_init_api": "lit_api_init"

  # void lit_free_api(LitState *state);
  "lit_free_api": "lit_api_destroy"

  # LitValue lit_get_global(LitState *state, LitString *name);
  "lit_get_global": "lit_state_getglobalvalue"

  # LitFunction * lit_get_global_function(LitState *state, LitString *name);
  "lit_get_global_function": "lit_state_getglobalfunction"

  # void lit_set_global(LitState *state, LitString *name, LitValue value);
  "lit_set_global": "lit_state_setglobal"

  # _Bool lit_global_exists(LitState *state, LitString *name);
  "lit_global_exists": "lit_state_hasglobal"

  # void lit_define_native(LitState *state, const char *name, LitNativeFunctionFn native);
  "lit_define_native": "lit_state_defnativefunc"

  # void lit_define_native_primitive(LitState *state, const char *name, LitNativePrimitiveFn native);
  "lit_define_native_primitive": "lit_state_defnativeprimitive"

  # LitValue lit_instance_get_method(LitState *state, LitValue callee, LitString *mthname);
  "lit_instance_get_method": "lit_state_getinstancemethod"

  # LitInterpretResult lit_instance_call_method(LitState *state, LitValue callee, LitString *mthname, LitValue *argv, size_t argc);
  "lit_instance_call_method": "lit_state_callinstancemethod"

  # double lit_check_number(LitVM *vm, LitValue *args, uint8_t arg_count, uint8_t id);
  "lit_check_number": "lit_value_checknumber"

  # double lit_get_number(LitVM *vm, LitValue *args, uint8_t arg_count, uint8_t id, double def);
  "lit_get_number": "lit_value_getnumber"

  # _Bool lit_check_bool(LitVM *vm, LitValue *args, uint8_t arg_count, uint8_t id);
  "lit_check_bool": "lit_value_checkbool"

  # _Bool lit_get_bool(LitVM *vm, LitValue *args, uint8_t arg_count, uint8_t id, _Bool def);
  "lit_get_bool": "lit_value_getbool"

  # const char * lit_check_string(LitVM *vm, LitValue *args, uint8_t arg_count, uint8_t id);
  "lit_check_string": "lit_value_checkstring"

  # const char * lit_get_string(LitVM *vm, LitValue *args, uint8_t arg_count, uint8_t id, const char *def);
  "lit_get_string": "lit_value_getstring"

  # LitString * lit_check_object_string(LitVM *vm, LitValue *args, uint8_t arg_count, uint8_t id);
  "lit_check_object_string": "lit_value_checkobjstring"

  # LitInstance * lit_check_instance(LitVM *vm, LitValue *args, uint8_t arg_count, uint8_t id);
  "lit_check_instance": "lit_value_checkinstance"

  # LitValue * lit_check_reference(LitVM *vm, LitValue *args, uint8_t arg_count, uint8_t id);
  "lit_check_reference": "lit_value_checkreference"

  # void lit_ensure_bool(LitVM *vm, LitValue value, const char *lit_emitter_raiseerror);
  "lit_ensure_bool": "lit_value_ensurebool"

  # void lit_ensure_string(LitVM *vm, LitValue value, const char *lit_emitter_raiseerror);
  "lit_ensure_string": "lit_value_ensurestring"

  # void lit_ensure_number(LitVM *vm, LitValue value, const char *lit_emitter_raiseerror);
  "lit_ensure_number": "lit_value_ensurenumber"

  # void lit_ensure_object_type(LitVM *vm, LitValue value, LitObjType type, const char *lit_emitter_raiseerror);
  "lit_ensure_object_type": "lit_value_ensureobjtype"

  # LitValue lit_get_field(LitState *state, LitTable *table, const char *name);
  "lit_get_field": "lit_state_getfield"

  # LitValue lit_get_map_field(LitState *state, LitMap *map, const char *name);
  "lit_get_map_field": "lit_state_getmapfield"

  # void lit_set_field(LitState *state, LitTable *table, const char *name, LitValue value);
  "lit_set_field": "lit_state_setfield"

  # void lit_set_map_field(LitState *state, LitMap *map, const char *name, LitValue value);
  "lit_set_map_field": "lit_state_setmapfield"

  # static _Bool ensure_fiber(LitVM *vm, LitFiber *fiber);
  "ensure_fiber": "lit_vm_ensurefiber"

  # LitInterpretResult lit_call_function(LitState *state, LitFunction *callee, LitValue *argv, uint8_t argc, _Bool ignfiber);
  "lit_call_function": "lit_state_callfunction"

  # LitInterpretResult lit_call_closure(LitState *state, LitClosure *callee, LitValue *argv, uint8_t argc, _Bool ignfiber);
  "lit_call_closure": "lit_state_callclosure"

  # LitInterpretResult lit_call_method(LitState *state, LitValue instance, LitValue callee, LitValue *argv, uint8_t argc, _Bool ignfiber);
  "lit_call_method": "lit_state_callmethod"

  # LitInterpretResult lit_call(LitState *state, LitValue callee, LitValue *argv, uint8_t argc, _Bool ignfiber);
  "lit_call": "lit_state_callvalue"

  # LitInterpretResult lit_find_and_call_method(LitState *state, LitValue callee, LitString *method_name, LitValue *argv, uint8_t argc, _Bool ignfiber);
  "lit_find_and_call_method": "lit_state_findandcallmethod"

  # LitString * lit_to_string(LitState *state, LitValue object);
  "lit_to_string": "lit_value_tostring"

  # LitValue lit_call_new(LitVM *vm, const char *name, LitValue *args, size_t argc, _Bool ignfiber);
  "lit_call_new": "lit_value_callnew"

